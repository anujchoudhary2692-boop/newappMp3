/**
 * Optional manual LAN override — leave empty; the app auto-discovers via Bonjour.
 * Only set this if Bonjour is blocked on your network.
 */
export const USE_PHYSICAL_DEVICE = true;
export const LAN_BACKEND_HOST: string = '';
